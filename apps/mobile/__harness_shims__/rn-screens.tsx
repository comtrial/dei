/**
 * react-native-screens stub — Playwright web harness only.
 * ui/dialog.tsx imports FullWindowOverlay (iOS only at runtime; harness is web).
 */
import { Fragment } from 'react';

export const FullWindowOverlay = Fragment;
export function enableScreens() {}
export const ScreenStack = Fragment;
export const Screen = Fragment;
export default { FullWindowOverlay, enableScreens };
