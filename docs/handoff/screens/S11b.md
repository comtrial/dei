# S11b — 촬영 미리보기

- **담당자**: C
- **route(파일)**: `app/(app)/room/[roomId]/upload-preview.tsx`
- **목적**: 방금 촬영한 3초 영상을 업로드 전 확인하는 단계. 잘못 올리기 방지 + 재촬영 1번 더 기회. BeReal/Locket 패턴.

## 의존 DS 컴포넌트
- `FullscreenVideo` — 자동 루프 영상 미리보기 (.video-preview inset:0 그라데이션 + ▶ 재생 오버레이)
- `IconButton` — 좌상단 원형 닫기 × (Floating overlay 컨트롤)
- `Badge` — 녹화 길이 배지 '2.3초' (상단중앙 backdrop-blur 반투명 pill)
- `Button` — secondary '다시 찍기' / primary '올리기' (비대칭 2-CTA, glass/backdrop-blur)
- `BottomActionBar` — 나란히 2-CTA 바 (secondary flex:1 / primary flex:1.4)
- `AlertDialog` — DiscardConfirm 다이얼로그 (조건부 — 영상 폐기 확인)
- `ProgressBar` — 업로드 진행 표시 (조건부)
- `Spinner` — 업로드 오버레이 + UI disable (조건부)
- `Text`

## 의존 데이터 / 서버(L1) / 정책(L2)
- **데이터**: 없음 (촬영 직후 로컬 영상 핸들 — 스키마 테이블 의존 없음)
- **서버(L1)**: 영상 업로드 (올리기 CTA → 업로드, 실패 시 S12)
- **정책(L2)**:
  - 편집 기능 MVP 제외 (자르기·필터·텍스트 X — 날것 정신)
  - 닫기 시 영상 폐기 confirm 강제
  - 업로드 진행 중 UI disable

## 발생 이벤트 (PostHog)
- 없음

## 주요 결정 (decisions)
- 자동 루프 재생 — 반복으로 확인.
- 2-CTA: secondary '다시 찍기' → S11 / primary '올리기' → 업로드 후 직전 화면(S10/S13).
- 편집 기능 X — 자르기·필터·텍스트 MVP 제외.
- 닫기(×) 시 영상 폐기 confirm — '영상이 사라져요. 정말 닫을까요?' 잘못 누름 방지.
- 업로드 진행 중 progress 표시 + UI disable. 성공 시 자동 직전 화면 전환.
- 업로드 실패 시 S12로 분기.
- 녹화 길이 배지 — '2.3초' 표시로 길이 인지.

## 와이어프레임
all-screens S11b
