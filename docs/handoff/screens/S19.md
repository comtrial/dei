# S19 — 본인 프로필 수정 (허브)
- **담당자**: B
- **route(파일)**: `app/(app)/my-profile.tsx`
- **목적**: S05 우상단 아바타 탭 → 진입. 본인 정보 수정 + 잔여 패스·환불·설정·계정 관리 모든 진입점이 모이는 허브 화면. PRD 미명시(묶음 6 신규). 묶음 6 모든 진입점의 hub(잔여 패스·환불·고객센터·탈퇴·알림·약관).

## 의존 DS 컴포넌트
- **TopNav** — 뒤로가기 + '내 프로필' 타이틀 + 저장 우측 액션(변경 시 활성, opacity 토글)
- **ProfileHero** — 90px 원형 아바타 + edit ✎ 배지 오버레이(탭 = 다시 촬영)
- **SettingsRow** — 프로필/설정·계정 row(k/v/arr), locked·danger 변형 + SectionGroup(sec-t uppercase 캡션 + grouped row) 그룹핑, LockRow(🔒 비활성 ink-4)
- **Card** — 잔여 바로 매치 PassCard(accent-soft + 원형 아이콘 + cta-mini 알약)
- **Button / Chip** — '더 사기'·'지금 시작' cta-mini 알약형 accent
- **Text**

## 의존 데이터 / 서버(L1) / 정책(L2)
### 데이터 (스키마 테이블)
- `profiles` — 닉네임/자기소개/MBTI/지역/성별/생년월일/아바타 (본인 row UPDATE)
- 닉네임 변경 throttle 메타 — `nickname_changed_at` 또는 동등(30일 제한 계산용, 테이블명 HTML 미명시)
- 잔여 바로 매치 패스 잔량 — passes/entitlements 류(테이블명 HTML 미명시)

### 서버 의존 (L1)
- 프로필 UPDATE RPC/엔드포인트 (HTML 미명시)
- 닉네임 30일 throttle 서버 검증 (HTML 미명시)
- 잔여 패스 조회 (HTML 미명시)

### 정책 의존 (L2)
- 닉네임 30일 1회 변경 제한 정책
- lock 필드 정책 (성별·생년월일·본인인증 변경 불가)
- 잔여 패스 0회 시 CTA 분기 ('지금 시작' vs '더 사기')

## 발생 이벤트 (PostHog)
- `S19:profile_hub_opened`
- `S19:nickname_change_throttled_30d`

## 주요 결정 (decisions)
- 섹션 3개: ① 프로필 정보(수정) ② 결제·바로 매치 ③ 설정·계정
- 수정 가능 필드: 닉네임(30일 1회 제한 안내) · 사진(다시 촬영) · 자기소개 · MBTI · 지역
- lock 필드: 성별·생년월일·본인인증 정보 (변경 불가)
- 잔여 패스 카드: 보유 N회 + '더 사기' CTA → S17. 0회면 '지금 시작' CTA
- 환불·고객센터·알림·약관·탈퇴 모두 ③ 섹션 row 메뉴
- 저장 — 상단 우측 '저장' 버튼, 변경사항 있을 때만 활성. 자동 저장 X
- 닉네임 30일 미만이면 수정 필드 disabled + '다음 변경 가능: YYYY.MM.DD' 안내

### 라우팅 메타
- pp-id S19. 진입: S05 우상단 아바타.
- 분기: 알림→S22 / 고객센터→S23 / 환불 요청→S23 / 회원 탈퇴→S20 / 더 사기→S17(잔여 패스 결제).

### Followups (pending)
- flow-card status: planned
- 닉네임 다음 변경 가능일 'YYYY.MM.DD' 표기 데이터 소스 미명시
- 약관 보기 row 목적지 화면 미명시
- '더 사기' → S17 결제 화면 연계 (본 그룹 밖)

## 와이어프레임
all-screens S19
