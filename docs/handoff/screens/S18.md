# S18 — 결제 실패 alert

- **담당자**: B
- **route(파일)**: `app/(app)/booster-failed.tsx`
- **목적**: S17 바로 매치 결제 실패(카드 거절·잔액 부족·네트워크 등) 시 표시하는 모달. 24h 제한이 그대로 유지됨을 명확히 알리고 다음 액션을 제시한다. 닫기 → S05 홈(제한 상태 그대로). NOTE: HTML 주석(line 593)·작업브리프의 'VLOG/Wrapped' 라벨은 stale — planning-panel SSOT 기준 S18 = '결제 실패 alert'. VLOG/Wrapped 화면은 영구 제거 확정(휘발 정책)이며 디바이스 모킹은 `.sPF`(payment-fail) 클래스를 사용한다.

## 의존 DS 컴포넌트
- **AlertDialog** — `.sPF` 반투명 오버레이(rgba(0,0,0,.55)) + behind brightness(.4) 흐림 + 중앙 정렬 modal(paper, r-lg)
- **Banner** — 경고 StatusIcon(danger-soft 배경 + danger fg, '!') 및 info-soft 안심 박스(✓ + '추가 페널티 없음' blue 카피)
- **Card** — PG 에러 코드 박스(bg-2 + font-mono error_code, 예: card_declined · insufficient_funds, PG: PortOne)
- **IconButton** — 에러 코드 박스 탭 = 클립보드 복사
- **Button** — StackedCtaList 3단 위계: primary '다른 결제 수단으로 재시도' / secondary '고객센터 문의'(→ S23) / tertiary '닫기 (홈으로)'(→ S05)
- **Text** — 헤딩('결제가 완료되지 않았어요')·설명('결제 도중 문제가 생겼어요. 24시간 제한은 그대로 유지돼요.')·'복사됨' 토스트 카피

## 의존 데이터 / 서버(L1) / 정책(L2)
- **데이터**: S17 결제 트랜잭션 실패 결과/에러 코드(PG error_code, 예: card_declined·insufficient_funds) 수신 · 24h 제한 상태 유지 확인(실패가 제한 시간에 영향 없음)
- **서버(L1)**: PG(PortOne) 실패 콜백 → 에러 코드 전달 · 재시도 시 S17 결제 경로 재진입 / 고객센터 폼(S23) 연결
- **정책(L2)**: 결제 실패 시 추가 페널티 없음 정책(제한 시간 증가 X) · 고객센터 진입 채널 정책 — 묶음 6에서 확정 예정(pending)

## 발생 이벤트 (PostHog)
- `payment_failure_alert_shown` (flow 카드 alt event; HTML 표기 'S18:payment_failure_alert_shown')

## 주요 결정 (decisions)
- '24h 제한 그대로 유지' 카피 (큐 컨텍스트 X)
- 추가 페널티 없음 안심 카피
- PG 에러 코드 노출 + 복사 버튼 — 고객센터 응대용
- 3개 옵션: 다른 결제 수단 / 고객센터 / 닫기(S05)
- 닫기 = S05 (24h 제한 유지)

## 와이어프레임
all-screens S18
